 <% include ../_partials/header %>

 <% include ../person/_partials/profile_left.ejs %>

          <div class="eight columns">
            <div class="nav-right">
              <ul>
                <li> 
                  <b>person:</b>
                </li>
                <li>
                    <a class="no-style-link selected" href="/@/<%= user.username %>/person/<%= person._id %>">description</a>
                </li>
                <li>
                    <a class="no-style-link" href="/@/<%= user.username %>/person/<%= person._id %>/info">information</a>
                </li>
                <li>
                    <a class="no-style-link" href="/@/<%= user.username %>/person/<%= person._id %>/groups">groups</a>
                </li>
                 <li>
                    <a class="no-style-link" href="#">events</a>
                </li>
            <!--     <li>
                    <a class="no-style-link" href="#">notes</a>
                </li>
                  <li>
                    <a class="no-style-link" href="#">history</a>
                </li> -->
              </ul>
              
            </div>
            <div>
            <!-- <div id="i"> -->
            <% if(person.short_description){ %>
               <div>
                <span class="title-info">Brief Description:</span><br>
                <p> 
                  <%= person.short_description %>
                </p>
              </div> 
            <% } %>
            

            <% if(person.description){ %>
              <div>
                <span class="title-info ">Detailed Description:</span><br>
                <%- person.description %>
              </div>
            <% } %>
            
            </div>
          </div>

      <!--  end row  -->
        </div>
    <!--  end container -->
    </div>


    <script>

      $("#button-profile-delete").on("click", function(){
         
          if (confirm("Are you sure you want to delete this entry?") == true) {

              var deletePath = $(this).attr('data-delete-path');
              var redirectPath = $(this).attr('data-redirect-path');
          
              $.post(deletePath, function() {
                window.location.href = redirectPath;
              });
          } else {
              return false;
          }

      })
    </script>


<!-- footer start -->

    
  </body>
</html>